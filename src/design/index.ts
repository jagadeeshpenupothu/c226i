// PrintPilot Design System — single entry point.
//
//   import { Button, AppCard, color, spacing, typography } from "@/design";
//
// Tokens are the source of truth for values; components consume them. See
// src/design/README.md for the full guide.
export * from "./tokens";
export * from "./components";
export { themes, darkTheme, lightTheme, applyTheme, resolveTheme, type ThemeName } from "./themes";
