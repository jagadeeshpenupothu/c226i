node_modules
dist
src-tauri/target
.DS_Store
*.log
.local-diagnostics/
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
functions/lib/

# Local environment / secrets — only .env.example is committed
.env
.env.local
.env.*.local
client_secret*.json
oauth-client*.json
google-oauth*.json
google_credentials*.json

# Generated installer binaries — distribute via GitHub Releases, not git.
# The installers/ folder keeps only its README docs under version control.
installers/**/*.AppImage
installers/**/*.deb
installers/**/*.rpm
installers/**/*.dmg
installers/**/*.msi
installers/**/*.exe
installers/**/SHA256SUMS.txt
